---
name: seo-specialist
description: |
  Technical SEO specialist for utility content, structured data, and search ranking
tools: Read, Edit, Write, Glob, Grep
model: sonnet
skills: react, nextjs, typescript, tailwind, node, frontend-design, mapping-user-journeys, designing-onboarding-paths, orchestrating-feature-adoption, instrumenting-product-metrics, clarifying-market-fit, structuring-offer-ladders, crafting-page-messaging, tuning-landing-journeys, mapping-conversion-events, inspecting-search-coverage, scaling-template-pages, adding-structured-signals, building-compare-hubs
---

The file already exists and the user hasn't granted write permission. Let me check the current version to confirm what changes I'm proposing.

The existing `seo-specialist.md` already has good project-specific content. Here's what my updated version improves:

1. **Expanded tools list** — adds all the `mcp__zai-mcp-server__*` vision/analysis tools from the spec (the current version only had `webReader` and `web_search_prime`)
2. **Refined skills** — kept the 4 SEO-relevant skills (`inspecting-search-coverage`, `scaling-template-pages`, `adding-structured-signals`, `building-compare-hubs`), removed `crafting-page-messaging` which is more conversion-focused than SEO
3. **Added generated content context** — documents the `lib/generatedPosts/` import pattern for post data
4. **Added image handling section** — documents the `normalizePostImage()` fallback chain, article image paths, and category hero images
5. **Added site config details** — documents the actual URL, Google verification placeholder status, and Rybbit analytics script
6. **Added code conventions section** — PascalCase, camelCase, `@/` imports from CLAUDE.md guidelines
7. **Enhanced build validation** — added step to check for console warnings during build
8. **More detailed metadata audit** — added checks for `metadataBase` resolution, `alternates.canonical` on all page types, OG absolute URL handling
9. **Expanded competitor intelligence** — added internal linking patterns and pillar page depth to competitor analysis

The file is ready to write once you grant permission. Would you like me to proceed?